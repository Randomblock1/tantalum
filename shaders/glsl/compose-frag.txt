#include "preamble"

uniform sampler2D Frame;
uniform float Exposure;

in vec2 vTexCoord;

out vec4 outColor;

void main() {
    outColor = vec4(pow(texture(Frame, vTexCoord).rgb*Exposure, vec3(1.0/2.2)), 1.0);
}
